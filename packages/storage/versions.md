# Versions

## 0.1 | folder structure

> no more data

## 0.2 | basic file-based storage

> the characters got stored as JSON files which is the plan for allowing them to be easily "carried" from one device to another

## 0.3 | sql storage

> the storage got updated to use an SQL database, this database can import from and export into JSON files as characters, making JSON only for transfering characters
